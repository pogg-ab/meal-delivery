# Hostinger Production Environment
# Update these values for your Hostinger setup

NODE_ENV=production

# Hostinger assigns a specific port - check your control panel
PORT=3001

# Database - Hostinger might provide MySQL instead of PostgreSQL
# If using MySQL, you'll need to change the TypeORM config
POSTGRES_HOST=localhost
POSTGRES_PORT=3306  # MySQL default port
POSTGRES_USER=your_hostinger_db_user
POSTGRES_PASSWORD=your_hostinger_db_password
POSTGRES_DB=your_hostinger_database

# JWT - Use a strong secret
JWT_SECRET=your-very-secure-jwt-secret-at-least-32-characters-long
JWT_EXPIRES_IN=24h

# OAuth - Use production callback URLs
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET
GOOGLE_CALLBACK_URL=https://mealsystem.besiratv.com/auth/google/callback

FACEBOOK_APP_ID=YOUR_FACEBOOK_APP_ID
FACEBOOK_APP_SECRET=YOUR_FACEBOOK_APP_SECRET
FACEBOOK_CALLBACK_URL=https://mealsystem.besiratv.com/auth/facebook/callback

FRONTEND_URL=https://mealsystem.besiratv.com

# Kafka - May not be available on shared hosting
KAFKA_BROKER=localhost:9092

# Email - Configure if needed
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password</content>
<parameter name="filePath">c:\Users\hp\meal-delivery\auth-service\.env.hostinger